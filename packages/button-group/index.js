import MeButtonGroup from '../button/src/button-group';

/* istanbul ignore next */
MeButtonGroup.install = function (Vue) {
  Vue.component(MeButtonGroup.name, MeButtonGroup);
};

export default MeButtonGroup;
