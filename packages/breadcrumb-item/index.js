import MeBreadcrumbItem from '../breadcrumb/src/breadcrumb-item';

/* istanbul ignore next */
MeBreadcrumbItem.install = function (Vue) {
  Vue.component(MeBreadcrumbItem.name, MeBreadcrumbItem);
};

export default MeBreadcrumbItem;
